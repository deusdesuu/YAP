# Мячин Валентин БАС-2 Лабораторная №5 (вариант 6)

# Задание 1

## Задача 1. Рекурсия

### Текст задачи

6. Указать все делители натурального числа n. 

### Алгоритм решения

- начиная с I = 1 спускаемся вглубь рекурсии и проверяем
- I <= floor(n // 2) - дошли ли до середины числа? (дальше нет смысла проверять)
  - если нет, то собираем результат (все делители, начиная с последнего найденного в список)
  - если да, то проверяем делимость N / I
    - если делится, то добавляем в список

### Тестирование

<img width="955" height="353" alt="image" src="https://github.com/user-attachments/assets/53c6d4e5-0f72-42bd-8479-7ac0c1990fe8" />




## Задача 2. Списки

### Текст задачи

6. Найти сумму чётных элементов списка целых чисел. 

### Алгоритм решения

- мапаем список по функции check_even()
- считаем сумму элементов списка
- возвращаем результат
- check_even(N):
  - четное -> N
  - нечетное -> 0

### Тестирование

Скриншоты результата работы программы

<img width="954" height="301" alt="image" src="https://github.com/user-attachments/assets/4bcfcf5a-1bce-4434-9c97-c3ae7d8df159" />




## Задача 3. Решить задачу на обработку списка

### Текст задачи

6. Определим множество как список без повторяющихся элементов. Доказать
закон де Моргана для объединения. 

### Алгоритм решения

- Прописываем основные функции для множеств:
  - a (не)принадлежит A
  - A + B
  - A & B
  - not A
    
- Принадлежит (in(X, A)):
  - рекурсивно проходим по множеству (списку), если находим элемент -> true
  
- A + B (union(A, B, Res)):
  - рекурсивно проходим по списку А
  - если елемента а из А нет в В, то добавляем в результат
  - результат изначально = В, вставляем а из А в начало списка
 
- A & B (intersect(A, B, Res)):
  - результат изначально []
  - если элемент b из В есть в А, то добавляем в результат
  - остальное так же как и А + В

- not A (negate(U, A, Res)):
  - имеем изначальное универсальное мн-во со всеми возможными элементами (чтобы было откуда их вычитать)
  - рекурсивно проходим по элементам универсального множества
  - если элемент есть в А - пропускаем
  - иначе добавляем в результат

- Прописываем функции de_morgan_left(right), чтобы потом показать что результаты равны:
  - left: not(A + B)
  - right: not(A) & not(B)

- Задаем U, A, B и проверяем закон
  
### Тестирование

Скриншоты результата работы программы

<img width="953" height="260" alt="image" src="https://github.com/user-attachments/assets/cfcbbcdd-a87f-46a4-9d67-010d58c59bc7" />



## Задача 4. Решить логическую задачу

### Текст задачи

6. Для полярной экспедиции из восьми претендентов А, В, С, D, Е, F, G и Н надо
отобрать шестерых специалистов: биолога, гидролога, синоптика, радиста,
механика и врача. Обязанности биолога могут выполнять Е и G, гидролога — В
и F, синоптика — F и G, радиста — Си D, механика — С и Н, врача — А и D. Хотя
некоторые претенденты владеют двумя специальностями, в экспедиции
каждый сможет выполнять только одну обязанность. Кого и кем следует взять
в экспедицию, если F не может ехать без В, D — без С и без Н, С не может ехать
одновременно с G, а A вместе с B? 

### Алгоритм решения

- Ставим условие: кто какие должности может занимать
- Ставим условие, что каждый должен делать что-то одно (люди не повторяются)
- Ставим условие: кто с кем (не)хочет быть в одной команде
- Формируем результат (все возможные, перебор через ; (в нашем случае только 1))

### Тестирование

Скриншоты результата работы программы

<img width="789" height="147" alt="image" src="https://github.com/user-attachments/assets/2871ced5-1a2e-483c-8e00-a735bdecd5f2" />
